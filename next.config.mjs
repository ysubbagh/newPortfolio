/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      // Specify the allowed domains for loading images
      domains: ['zappysys.com', 'upload.wikimedia.org', 'cdn.jsdelivr.net', 'as1.ftcdn.net', 'cdn.pixabay.com', 'github.githubassets.com', 'ftcdn.net'],
    },
};

export default nextConfig;

  